const split = require( './split' );

module.exports = command => {

    if( !command ) return false;

    const peices = split( /[ \t]+/ );
    const name = pieces[ 0 ];

    if( /^[a-zA-Z_$][a-zA-Z0-9_$]/ )


    return {
    }
};
